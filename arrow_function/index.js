// aroe function - a conice way to write expression good for simple function that you use only once (parameter ) => some code

const number = [1,2,3,4,5,6];
const squre = number.filter((e) => e % 2 === 0);
console.log(squre);